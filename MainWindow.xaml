<Window x:Class="SelectXYZ_Cheat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SelectXYZ_Cheat"
        mc:Ignorable="d"
        Title="Pxrfxtly Slotted" 
        Height="600" Width="800"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter x:Name="contentPresenter" 
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            Content="{TemplateBinding Content}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            Margin="{TemplateBinding Padding}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF2A2A2A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Margin" Value="0 5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="checkBoxBorder"
                                    Width="16" Height="16"
                                    CornerRadius="3"
                                    BorderThickness="1"
                                    BorderBrush="#FFE53E3E"
                                    Background="#FF1E1E1E"
                                    Margin="0,0,5,0">
                                <Path x:Name="checkMark"
                                      Data="M 0 6 L 6 12 L 12 0"
                                      Stroke="#FFE53E3E"
                                      StrokeThickness="2"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Margin="5,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="#FFFF6B6B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Toggle Button Template -->
        <ControlTemplate x:Key="ComboBoxToggleButtonTemplate" TargetType="ToggleButton">
            <Border x:Name="templateRoot"
                    Background="Transparent"
                    BorderThickness="0"
                    SnapsToDevicePixels="True">
                <Path x:Name="arrow"
                      Data="M 0 0 L 4 4 L 8 0 Z"
                      Fill="#FFE53E3E"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Margin="0 0 5 0"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="arrow" Property="Fill" Value="#FFFF6B6B"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="arrow" Property="Fill" Value="#FFE53E3E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter TargetName="arrow" Property="Fill" Value="#FF555555"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFE53E3E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="0 5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="contentPresenter"
                                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                    Content="{TemplateBinding SelectionBoxItem}"
                                                    ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                    Margin="{TemplateBinding Padding}"
                                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    <ToggleButton x:Name="toggleButton"
                                                Grid.Column="1"
                                                Background="Transparent"
                                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                Template="{StaticResource ComboBoxToggleButtonTemplate}"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup"
                                  AllowsTransparency="True"
                                  Placement="Bottom"
                                  Focusable="False"
                                  PopupAnimation="Slide"
                                  IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                <Border x:Name="dropDownBorder"
                                        Background="#FF1E1E1E"
                                        BorderBrush="#FFE53E3E"
                                        BorderThickness="1"
                                        CornerRadius="0 0 3 3">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <ItemsPresenter x:Name="ItemsPresenter"
                                                      KeyboardNavigation.DirectionalNavigation="Contained"
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#FFFF6B6B"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Label Style -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Margin" Value="0 5 0 0"/>
        </Style>

        <!-- GroupBox Style -->
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
            <Setter Property="BorderBrush" Value="#FFE53E3E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border CornerRadius="5"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Border Background="#FFE53E3E"
                                        CornerRadius="3 3 0 0"
                                        Padding="5 2 5 2"
                                        Margin="5 0 5 0">
                                    <ContentPresenter ContentSource="Header"
                                                    RecognizesAccessKey="True"
                                                    HorizontalAlignment="Center"/>
                                </Border>
                                <ContentPresenter Grid.Row="1"
                                                Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ColorPickerControl Style -->
        <Style TargetType="{x:Type local:ColorPickerControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type local:ColorPickerControl}">
                        <StackPanel Orientation="Horizontal" Margin="0 5">
                            <Label Content="{TemplateBinding Label}" Width="120"/>
                            <Border x:Name="PART_ColorDisplay" 
                                    Width="20" Height="20" CornerRadius="3" 
                                    Background="{Binding SelectedColor, RelativeSource={RelativeSource TemplatedParent}}"
                                    BorderBrush="#FFE53E3E" BorderThickness="1"
                                    Margin="5 0 0 0" Cursor="Hand">
                                <Border.ToolTip>
                                    <ToolTip>
                                        <TextBlock Text="Click to change color"/>
                                    </ToolTip>
                                </Border.ToolTip>
                            </Border>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="#FF121212" BorderBrush="#FFE53E3E" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Background="#FF1E1E1E" CornerRadius="10 10 0 0" Height="50">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15 0">
                        <TextBlock Text="Pxrfxtly " FontSize="18" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Slotted" FontSize="18" FontWeight="Bold" Foreground="#FFE53E3E"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0 0 15 0" Grid.Column="1">
                        <Button Content="_" Width="30" Click="MinimizeButton_Click"/>
                        <Button Content="X" Width="30" Margin="5 0 0 0" Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Tab Control -->
            <TabControl Grid.Row="1" Background="#FF0F0F0F" BorderThickness="0" Margin="10 5 10 10">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" Background="#FF1A1A1A" Margin="0 0 2 0" CornerRadius="5 5 0 0">
                                        <ContentPresenter x:Name="ContentSite"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Center"
                                                          ContentSource="Header"
                                                          Margin="15 8"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#FFE53E3E"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="False">
                                            <Setter Property="Foreground" Value="#FFCCCCCC"/>
                                            <Setter TargetName="Border" Property="Background" Value="#FF1A1A1A"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter TargetName="Border" Property="Background" Value="#FF2A2A2A"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Border" Property="Background" Value="#FFFF4444"/>
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Padding" Value="10 5"/>
                    </Style>
                </TabControl.Resources>

                <!-- Aimbot Tab -->
                <TabItem Header="Aimbot">
                    <Grid Background="Transparent">
                        <TextBlock Text="Aimbot settings will go here" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   Foreground="White"/>
                    </Grid>
                </TabItem>

                <!-- Visuals Tab (Selected by Default) -->
                <TabItem Header="Visuals" IsSelected="True">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Panel - ESP Settings -->
                        <GroupBox Grid.Column="0" Header="Visuals Settings">
                            <StackPanel>
                                <Label Content="ESP Box Type:"/>
                                <ComboBox x:Name="BoxTypeComboBox" SelectedIndex="0" SelectedValue="{Binding BoxType}">
                                    <ComboBoxItem Content="None"/>
                                    <ComboBoxItem Content="Box2D"/>
                                    <ComboBoxItem Content="Box3D"/>
                                    <ComboBoxItem Content="CornerBox"/>
                                </ComboBox>

                                <CheckBox x:Name="SkeletonESPCheckBox" Content="Skeleton ESP" IsChecked="{Binding SkeletonESP}" Click="OnESPSettingChanged"/>
                                <CheckBox x:Name="HealthBarCheckBox" Content="Health Bar" IsChecked="{Binding HealthBar}" Click="OnESPSettingChanged"/>
                                <CheckBox x:Name="HealthNumberCheckBox" Content="Health Number" IsChecked="{Binding HealthNumber}" Click="OnESPSettingChanged"/>
                                <CheckBox x:Name="PlayerNameCheckBox" Content="Player Name" IsChecked="{Binding PlayerName}" Click="OnESPSettingChanged"/>
                                <CheckBox x:Name="DistanceESPCheckBox" Content="Distance ESP" IsChecked="{Binding DistanceESP}" Click="OnESPSettingChanged"/>
                                <CheckBox x:Name="HeadCircleCheckBox" Content="Head Circle" IsChecked="{Binding HeadCircle}" Click="OnESPSettingChanged"/>
                                <CheckBox x:Name="SonarESPCheckBox" Content="Sonar ESP" IsChecked="{Binding SonarESP}" Click="OnESPSettingChanged"/>
                                <CheckBox x:Name="ChinaHatCheckBox" Content="China Hat" IsChecked="{Binding ChinaHat}" Click="OnESPSettingChanged"/>
                                <CheckBox x:Name="ChamsCheckBox" Content="Chams" IsChecked="{Binding Chams}" Click="OnESPSettingChanged"/>
                                <CheckBox x:Name="BoxGlowCheckBox" Content="Box Glow (Bounding Only)" IsChecked="{Binding BoxGlow}" Click="OnESPSettingChanged"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Right Panel - Color Pickers -->
                        <GroupBox Grid.Column="1" Header="Colors">
                            <ScrollViewer>
                                <StackPanel>
                                    <local:ColorPickerControl Label="Skeleton Color" DefaultColor="White"/>
                                    <local:ColorPickerControl Label="Corner Box Color" DefaultColor="White"/>
                                    <local:ColorPickerControl Label="Health Bar Color" DefaultColor="Green"/>
                                    <local:ColorPickerControl Label="Box Fill Color" DefaultColor="Black"/>
                                    <local:ColorPickerControl Label="Head Circle Color" DefaultColor="White"/>
                                    <local:ColorPickerControl Label="3D Box Color" DefaultColor="Black"/>
                                    <local:ColorPickerControl Label="Outline Color" DefaultColor="Red"/>
                                    <local:ColorPickerControl Label="Chams Color" DefaultColor="Cyan"/>
                                    <local:ColorPickerControl Label="ChinaHat Color" DefaultColor="Red"/>
                                    <local:ColorPickerControl Label="Sonar Color" DefaultColor="Cyan"/>
                                    <local:ColorPickerControl Label="Box Color" DefaultColor="Red"/>
                                    <local:ColorPickerControl Label="Name Color" DefaultColor="White"/>
                                    <local:ColorPickerControl Label="Distance Color" DefaultColor="Yellow"/>
                                </StackPanel>
                            </ScrollViewer>
                        </GroupBox>
                    </Grid>
                </TabItem>

                <!-- Miscellaneous Tab -->
                <TabItem Header="Miscellaneous">
                    <Grid Background="Transparent">
                        <TextBlock Text="Miscellaneous settings will go here" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   Foreground="White"/>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Border>
</Window>